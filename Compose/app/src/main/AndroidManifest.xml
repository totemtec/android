<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Compose"
        tools:targetApi="31">
        <activity
            android:name=".state.NewsActivity"
            android:exported="true"
            android:theme="@style/Theme.Compose" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".state.SimpleStateActivity"
            android:exported="true"
            android:theme="@style/Theme.Compose">

        </activity>
        <activity
            android:name=".WebViewActivity"
            android:exported="true"
            android:theme="@style/Theme.Compose" />
        <activity
            android:name=".DialogActivity"
            android:theme="@style/Theme.Compose" />
        <activity android:name=".navigation.GardenActivity" />
        <activity android:name=".navigation.NavigationActivity" />
        <activity android:name=".SnackbarActivity" />
        <activity android:name=".TextFieldActivity" />
        <activity android:name=".ConstraintLayoutActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ColumnActivity" />
        <activity android:name=".RowActivity" />
        <activity android:name=".BoxActivity" />
        <activity android:name=".TextActivity" />
        <activity android:name=".ButtonActivity" />
    </application>

</manifest>